
function messageTheme(doc) {

	var title = doc.title;
	var url = doc.url;
	var summary = doc.summary;
	var categories = doc.categories;

    var msg = "<br>malaka:"+title+"<br>malaka<br>";
    
	return msg;
}

